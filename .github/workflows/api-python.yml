name: Python API CI/CD

on:
  push:
    paths: ['api-python/**']
    branches: [main]
  pull_request:
    paths: ['api-python/**']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: api-python/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: api-python

      - name: Lint
        run: pip install ruff && ruff check .
        working-directory: api-python

      # Uncomment and configure once Azure App Service is set up:
      # - name: Deploy to Azure App Service
      #   uses: azure/webapps-deploy@v3
      #   with:
      #     app-name: ss-python-api
      #     publish-profile: ${{ secrets.AZURE_PYTHON_PUBLISH_PROFILE }}
      #     package: api-python
